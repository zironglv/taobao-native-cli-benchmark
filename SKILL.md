---
name: taobao-native-cli-benchmark
description: 淘宝桌面版CLI工具评测框架。用于系统化评测淘宝桌面版CLI工具的各项功能，生成专业的技术评测报告。
version: v1.0.0
metadata:
  openclaw:
    emoji: "📊"
    author: "CoPaw AI"
    created: "2026-03-23"
    updated: "2026-03-23"
---

# 淘宝桌面版CLI工具评测框架 v1.0.0

## 概述

本skill提供一套系统化的评测框架，用于测试淘宝桌面版CLI工具的各项功能，并生成专业的技术评测报告。

**v1.0.0 更新内容**：
- 重构为CLI工具评测框架
- 钉钉文档改为每次评测创建新文档，文档名带时间戳
- 新增详细工具调用明细记录要求
- 新增任务10：图片搜索找性价比最高商品
- 新增任务11：订单评价（填写内容但不提交）

---

## ⚠️ 执行原则（必须遵守）

### 🔴 核心原则：优先使用淘宝桌面版 CLI 工具

**在所有淘宝桌面版相关操作中，必须优先使用淘宝桌面版 CLI 工具（taobao-native），而非浏览器自动化或其他方式。**

| 原则 | 说明 |
|------|------|
| **官方工具** | 淘宝桌面版 CLI 工具由淘宝官方提供，性能和效率有保障 |
| **优先使用** | 所有导航、搜索、加购、订单、聊天等操作必须使用 CLI 工具 |
| **问题记录** | 如遇 CLI 工具问题，记录到报告中，便于反馈给官方优化 |
| **禁止替代** | 禁止使用 browser_use 等浏览器自动化工具替代 CLI 工具 |

**可用 CLI 工具清单**（共21个）：

| 分类 | 工具 |
|------|------|
| 导航 | `navigate`, `navigate_to_url`, `close_page` |
| 页面读取 | `read_page_content`, `scan_page_elements`, `scroll_page`, `get_current_tab` |
| 交互 | `click_element`, `input_text` |
| 搜索&商品 | `search_products`, `image_search`, `get_product_skus`, `add_to_cart`, `get_browse_history`, `submit_product_rating` |
| 旺旺聊天 | `open_chat`, `send_chat_message` |
| 其他 | `launch`, `inspect_page` |

### 原子性执行

**评测任务一旦开始，必须完整执行完成，不可中断。**

| 原则 | 说明 |
|------|------|
| **不可中断** | 开始评测后，必须完成所有11个任务 + 生成报告 |
| **完整流程** | 初始化 → 任务1-11 → 截图收集 → 报告生成 → 创建钉钉文档 |
| **状态跟踪** | 每个任务完成后记录 checkpoint，便于恢复 |
| **详细记录** | 每个工具调用必须记录完整参数和返回结果 |

---

## 📝 详细记录规范

### 工具调用日志格式

每次工具调用后，必须将完整信息保存到日志文件：

**文件位置**：`~/.copaw/tasks/benchmark_{id}/calls/`

**命名规则**：`{task_id}_{step}_{tool_name}.json`

**日志格式**：
```json
{
  "call_id": "T1_S1_navigate",
  "task_id": 1,
  "step": 1,
  "tool": "navigate",
  "params": {
    "page": "home"
  },
  "result": {
    "success": true,
    "page": "home",
    "url": "https://pages-fast.m.taobao.com/..."
  },
  "start_time": "2026-03-23 12:30:00.123",
  "end_time": "2026-03-23 12:30:02.456",
  "duration_ms": 2333,
  "success": true,
  "screenshot": "T1_S1_homepage.png",
  "error": null
}
```

### 截图关联规则

| 截图命名 | 关联调用 | 说明 |
|----------|----------|------|
| `T{task_id}_S{step}_{desc}.png` | 对应的调用日志 | 操作后截图 |
| `T{task_id}_issue_{n}.png` | 问题截图 | 发现问题时截图 |

### 任务完成记录格式

每个任务完成后，生成任务详情文件：

**文件位置**：`~/.copaw/tasks/benchmark_{id}/tasks/task_{id}.json`

```json
{
  "task_id": 1,
  "task_name": "淘金币签到",
  "weight": 0.10,
  "start_time": "2026-03-23 12:30:00",
  "end_time": "2026-03-23 12:32:30",
  "duration_seconds": 150,
  "call_count": 5,
  "score": 9.0,
  "status": "completed",
  "calls": [
    {
      "step": 1,
      "tool": "navigate",
      "params": {"page": "home"},
      "result_summary": "成功导航到首页",
      "duration_ms": 2333
    }
  ],
  "screenshots": ["T1_S1_homepage.png", "T1_S2_coin_popup.png"],
  "data": {
    "coins": 10003,
    "can_deduct": 100
  },
  "issues": [],
  "notes": "成功完成淘金币签到流程"
}
```

---

## 评测任务清单

### 任务1：淘金币签到（权重 10%）

**测试目标**：验证导航、元素识别、点击操作的稳定性

**测试步骤**：
1. `taobao-native navigate` → 首页
2. `taobao-native scan_page_elements` → 识别淘金币入口
3. `taobao-native click_element` → 进入淘金币页面
4. `taobao-native read_page_content` → 读取金币数量
5. 完成签到任务
6. 验证金币增加

**评分标准**：
| 指标 | 分值 |
|------|------|
| 导航成功 | 2分 |
| 元素识别准确 | 2分 |
| 点击操作成功 | 2分 |
| 金币增加验证 | 2分 |
| 流程顺畅度 | 2分 |

---

### 任务2：商品搜索+对比+加购（权重 14%）

**测试目标**：验证搜索、详情查看、SKU选择、加购流程

**测试步骤**：
1. `taobao-native search_products` → 搜索关键词
2. `taobao-native read_page_content` → 读取搜索结果
3. 筛选商品进行对比
4. `taobao-native get_product_skus` → 获取SKU规格
5. `taobao-native add_to_cart` → 加入购物车

**评分标准**：
| 指标 | 分值 |
|------|------|
| 搜索返回结果 | 2分 |
| 商品详情页导航 | 2分 |
| 信息提取完整 | 2分 |
| SKU选择准确 | 2分 |
| 加购成功 | 2分 |

---

### 任务3：订单管理（权重 14%）

**测试目标**：验证订单页面导航、状态筛选功能

**测试步骤**：
1. `taobao-native navigate` → 订单页面
2. `taobao-native scan_page_elements` → 识别筛选标签
3. 测试筛选功能
4. `taobao-native read_page_content` → 读取订单列表

**评分标准**：
| 指标 | 分值 |
|------|------|
| 订单页面导航 | 2分 |
| 筛选标签识别 | 2分 |
| 筛选功能正常 | 2分 |
| 订单信息读取 | 2分 |
| 页面切换流畅 | 2分 |

---

### 任务4：获取购物车以及降价信息（权重 14%）

**测试目标**：验证购物车导航、商品列表读取、降价信息提取

**测试步骤**：
1. `taobao-native navigate` → 购物车页面
2. `taobao-native read_page_content` → 读取商品列表
3. 统计购物车商品
4. 识别降价商品

**评分标准**：
| 指标 | 分值 |
|------|------|
| 购物车导航成功 | 2分 |
| 商品列表读取完整 | 2分 |
| 降价标签点击成功 | 2分 |
| 降价信息提取准确 | 2分 |
| 数据记录完整 | 2分 |

---

### 任务5：客服咨询对话（权重 10%）

**测试目标**：验证客服咨询功能

**测试步骤**：
1. `taobao-native search_products` → 搜索商品
2. `taobao-native open_chat` → 进入客服对话
3. 发送咨询消息
4. 等待回复

**评分标准**：
| 指标 | 分值 |
|------|------|
| 商品搜索成功 | 2分 |
| 进入客服对话 | 2分 |
| 消息发送成功 | 2分 |
| 客服回复接收 | 2分 |
| 对话记录完整 | 2分 |

---

### 任务6：图片OCR识别与淘宝加购（权重 4%）

**测试目标**：验证图片OCR识别、商品搜索、价格对比、加购流程

**测试图片**：`https://img.alicdn.com/imgextra/i2/385132127/O1CN01vV3hBP1RaDYS7eteZ_!!4611686018427381343-2-item_pic.png_.webp`

**测试步骤**：

#### 阶段1：OCR识别商品图片

1. **下载图片**：将测试图片下载到本地临时目录
2. **OCR识别**：使用OCR工具识别图片中的商品信息
   - 提取商品名称/关键词
   - 提取SKU规格（如有）
   - 记录识别结果

#### 阶段2：淘宝搜索商品

1. `taobao-native search_products` → 使用OCR提取的关键词搜索
2. `taobao-native read_page_content` → 读取搜索结果列表
3. 记录商品列表（标题、价格、店铺、链接）

#### 阶段3：价格对比与选品

1. 解析搜索结果中的价格信息
2. 按价格排序，找到最便宜的商品
3. 记录最低价商品信息

#### 阶段4：加购最便宜商品

1. `taobao-native add_to_cart` → 将最便宜的商品加入购物车
2. 验证加购成功

**评分标准**：
| 指标 | 分值 |
|------|------|
| 图片OCR识别成功 | 2分 |
| 关键词提取准确 | 1分 |
| 搜索返回结果 | 1分 |
| 价格对比正确 | 2分 |
| 加购最便宜商品成功 | 2分 |
| 数据记录完整 | 1分 |
| 流程顺畅度 | 1分 |

---

### 任务7：购物车价格监控设置（权重 4%）

**测试目标**：验证价格监控功能

**测试步骤**：
1. `taobao-native navigate` → 购物车
2. `taobao-native read_page_content` → 读取降价信息
3. 记录价格监控数据

**评分标准**：
| 指标 | 分值 |
|------|------|
| 购物车导航成功 | 2分 |
| 降价信息显示 | 2分 |
| 价格保护服务 | 2分 |
| 监控入口识别 | 2分 |
| 数据完整性 | 2分 |

---

### 任务8：退货退款申请（权重 8%）

**测试目标**：验证售后流程完整性，包括订单查找、申请提交、状态跟踪

**⚠️ 重要说明**：
- 此任务会**真实提交退货退款申请**，请在测试环境或确认后执行

**测试步骤**：

#### 阶段1：查找已成交订单

1. `taobao-native navigate` → `{"page": "order"}`
2. `taobao-native scan_page_elements` → 识别订单状态筛选标签
3. `taobao-native click_element` → 点击"已完成"或"已成交"筛选
4. `taobao-native read_page_content` → 读取订单列表
5. 找到最新已成交订单，记录订单号

#### 阶段2：提交退货退款申请

1. `taobao-native click_element` → 点击订单的"申请售后"按钮
2. `taobao-native scan_page_elements` → 识别售后类型选项
3. `taobao-native click_element` → 选择"退货退款"
4. `taobao-native click_element` → 选择退货原因
5. `taobao-native type` → 填写退货说明（可选）
6. `taobao-native click_element` → 提交申请
7. `taobao-native read_page_content` → 读取申请结果

**评分标准**：
| 指标 | 分值 |
|------|------|
| 订单查找成功 | 2分 |
| 售后入口识别 | 2分 |
| 申请流程完成 | 2分 |
| 结果通知正确 | 2分 |
| 定时任务设置（如需要） | 2分 |

---

### 任务9：收藏夹商家上新检查（权重 6%）

**测试目标**：验证收藏夹导航、商家上新状态检测、新品信息提取和通知功能

**测试步骤**：

#### 阶段1：进入收藏夹

1. `taobao-native navigate` → `{"page": "collect"}` 进入收藏夹页面
2. `taobao-native scan_page_elements` → 识别页面结构
3. `taobao-native click_element` → 切换到"关注商家"标签
4. `taobao-native read_page_content` → 读取关注的商家列表

#### 阶段2：检查商家上新状态

1. `taobao-native scan_page_elements` → 扫描商家列表，查找"上新"标签
2. 记录有上新的商家

#### 阶段3：获取新品详情

1. `taobao-native click_element` → 进入商家店铺或新品页面
2. `taobao-native read_page_content` → 读取新品列表
3. 提取新品信息

**评分标准**：
| 指标 | 分值 |
|------|------|
| 收藏夹导航成功 | 1分 |
| 商家列表识别准确 | 1分 |
| 上新状态检测正确 | 2分 |
| 新品信息提取完整 | 2分 |
| 筛选排序合理 | 2分 |
| 通知格式清晰 | 1分 |
| 流程顺畅度 | 1分 |

---

### 任务10：图片搜索找性价比最高商品（权重 8%）

**测试目标**：验证图片搜索功能、商品对比、性价比分析能力

**测试图片**：`https://img.alicdn.com/bao/uploaded/i2/3942407247/O1CN01bmfx1i23PBPUKv9aO_!!3942407247.jpg`

**测试步骤**：

#### 阶段1：下载测试图片

1. 下载测试图片到本地临时目录
2. 记录图片路径

#### 阶段2：执行图片搜索

1. `taobao-native image_search` → 使用测试图片进行搜索
2. `taobao-native read_page_content` → 读取搜索结果
3. 记录搜索结果列表

#### 阶段3：性价比分析

1. 解析搜索结果，提取关键信息：
   - 商品标题
   - 价格
   - 销量
   - 店铺名称
   - 商品评分/评价数（如有）
2. 计算性价比指标（可参考：价格/销量比、评价数等）
3. 选出性价比最高的商品

#### 阶段4：返回结果

返回性价比最高商品的信息：
```json
{
  "title": "商品标题",
  "price": 99.00,
  "sales": 1000,
  "shop": "店铺名称",
  "item_id": "xxx",
  "性价比评分": 8.5,
  "选择理由": "价格适中，销量高，评价好"
}
```

**评分标准**：
| 指标 | 分值 |
|------|------|
| 图片下载成功 | 1分 |
| 图片搜索执行成功 | 2分 |
| 搜索结果读取完整 | 2分 |
| 性价比分析合理 | 2分 |
| 结果返回准确 | 2分 |
| 流程顺畅度 | 1分 |

---

### 任务11：订单评价（权重 8%）

**测试目标**：验证订单评价流程、内容填写功能

**⚠️ 重要说明**：
- 此任务**仅填写评价内容，禁止提交**
- 完成内容填写即视为任务完成

**测试步骤**：

#### 阶段1：查找已成交订单

1. `taobao-native navigate` → `{"page": "order"}`
2. `taobao-native scan_page_elements` → 识别订单状态筛选标签
3. `taobao-native click_element` → 点击"已完成"或"已成交"筛选
4. `taobao-native read_page_content` → 读取订单列表
5. 选择一个已成交订单

#### 阶段2：进入评价页面

1. `taobao-native click_element` → 点击"评价"按钮
2. `taobao-native scan_page_elements` → 识别评价页面元素
3. 记录可评价的项目（商品评分、物流评分、服务评分等）

#### 阶段3：填写评价内容

1. `taobao-native click_element` → 选择评分（如5星好评）
2. `taobao-native type` → 填写评价文字内容
   - 示例内容："商品质量不错，物流很快，卖家服务态度好。"
3. 确认内容已填写成功

#### 阶段4：截图确认（禁止提交）

1. 截图保存当前评价填写状态
2. **停止操作，不点击提交按钮**
3. 告知用户：已填写评价内容，未提交

**数据记录格式**：
```json
{
  "order_id": "订单号",
  "order_name": "订单商品名称",
  "rating": 5,
  "content": "填写的评价内容",
  "submitted": false,
  "note": "评价内容已填写，未提交"
}
```

**评分标准**：
| 指标 | 分值 |
|------|------|
| 订单查找成功 | 2分 |
| 评价入口识别 | 2分 |
| 评分选择成功 | 2分 |
| 评价内容填写成功 | 2分 |
| 未提交确认 | 2分 |

---

## 评测流程

### 1. 初始化评测任务

```bash
# 创建评测任务目录结构
BENCHMARK_ID="benchmark_$(date +%Y%m%d_%H%M%S)"
mkdir -p ~/.copaw/tasks/$BENCHMARK_ID/{screenshots,calls,tasks}

# 创建初始状态文件
cat > ~/.copaw/tasks/$BENCHMARK_ID/status.json << 'EOF'
{
  "benchmark_id": "...",
  "version": "1.0.0",
  "start_time": "...",
  "status": "running",
  "current_task": 0,
  "tasks": [...]
}
EOF
```

### 2. 执行评测任务（详细记录）

**每个工具调用必须执行以下步骤**：

```bash
# 1. 记录开始时间
START_TIME=$(date +%s%3N)

# 2. 执行工具调用（结果保存到变量）

# 3. 记录结束时间
END_TIME=$(date +%s%3N)

# 4. 保存完整日志
cat > ~/.copaw/tasks/$BENCHMARK_ID/calls/T{task_id}_S{step}_{tool}.json << 'EOF'
{
  "call_id": "T1_S1_navigate",
  "start_time": "...",
  "end_time": "...",
  "duration_ms": ...,
  "tool": "navigate",
  "params": {...},
  "result": {...},
  "success": true,
  "screenshot": "..."
}
EOF

# 5. 截图
screencapture -x ~/.copaw/tasks/$BENCHMARK_ID/screenshots/T{task_id}_S{step}_{desc}.png
```

### 3. 生成评测报告

**报告必须包含**：

1. **整体小结**
   - 评测概览
   - 总体评分
   - 任务完成度
   - 工具调用统计
   - 问题汇总

2. **分任务详情**（每个任务必须包含）
   - 任务概要表
   - **执行流程表**（详细记录每个工具调用）
   - **工具调用日志**（完整的JSON返回结果代码块）
   - 过程截图（嵌入文档）
   - 数据结果
   - 问题分析

3. **技术分析**
   - 工具调用统计
   - 性能指标
   - 问题清单

4. **附录**
   - 截图清单
   - 完整调用日志

### 4. 创建钉钉文档并记录报告

**评测完成后，必须创建新的钉钉文档记录报告**：

```bash
# 生成文档标题（带时间戳）
DOC_TITLE="淘宝桌面版CLI评测报告_$(date +%Y%m%d_%H%M%S)"

# 读取本地报告内容
REPORT=$(cat ~/.copaw/tasks/$BENCHMARK_ID/评测报告.md)

# 创建新文档
mcporter call 钉钉文档 create_document --args "{
  \"title\": \"$DOC_TITLE\",
  \"markdown\": \"$REPORT\"
}"
```

**文档命名规则**：
- 格式：`淘宝桌面版CLI评测报告_YYYYMMDD_HHMMSS`
- 示例：`淘宝桌面版CLI评测报告_20260323_143052`

**必须记录的内容**：
1. 完整的评测报告
2. 每个工具调用的明细：
   - 调用时间
   - 工具名称
   - 输入参数（完整JSON）
   - 输出结果（完整JSON）
   - 耗时
   - 截图链接
3. 便于技术人员回溯排查问题

### 5. 完整评测流程总结

```
1. 初始化评测任务（创建目录、状态文件）
2. 执行任务1-11（每个任务详细记录日志和截图）
3. 生成本地评测报告（完整的Markdown格式）
4. 创建新钉钉文档（文档名带时间戳）
5. 将完整报告写入钉钉文档
6. 确认创建成功
7. 清理临时文件（可选）
```

---

## 📝 钉钉文档记录规范

### 核心原则

**每次评测必须创建新的钉钉文档，便于区分和追溯**：

| 规则 | 说明 |
|------|------|
| **创建新文档** | 每次评测创建一个新文档，不追加到已有文档 |
| **文档名带时间戳** | 格式：`淘宝桌面版CLI评测报告_YYYYMMDD_HHMMSS` |
| **完整报告** | 包含完整的评测报告内容 |
| **工具调用明细** | 必须记录每个工具调用的输入输出 |
| **便于回溯** | 方便技术人员排查问题 |

---

## ⚠️ 报告详细要求（必须遵守）

### 报告必须包含的内容

钉钉文档评测报告**必须**展示每个任务的具体执行过程，包含以下内容：

#### 一、整体小结部分

| 章节 | 必备内容 |
|------|----------|
| 1.1 评测概览表 | 总得分、完成率、任务数、总耗时、工具调用总次数、成功率 |
| 1.2 任务完成度表 | 每个任务的权重、得分、满分、状态 |
| 1.3 工具调用总览表 | 每个工具的调用次数、成功率、主要用途 |
| 1.4 问题汇总表 | 问题ID、所属任务、问题描述、严重程度 |

#### 二、分任务详情部分

**每个任务必须包含以下章节：**

##### 2.X.1 任务概要表
```markdown
| 项目 | 内容 |
|------|------|
| **任务目标** | 该任务要验证什么功能 |
| **测试结果** | ✅ 通过 / ⚠️ 部分通过 / ❌ 失败 |
| **得分** | X/10 |
| **耗时** | 约XX秒 |
```

##### 2.X.2 执行流程表
```markdown
| 步骤 | 工具 | 参数 | 结果 | 耗时 |
|------|------|------|------|------|
| 1 | navigate | {"page":"home"} | ✅ 成功 | 1200ms |
| 2 | scan_page_elements | {"filter":"金币"} | ✅ 识别到 | 800ms |
| ... | ... | ... | ... | ... |
```

**重点**：执行流程表**必须**展示：
- 每个步骤使用的工具名称
- 每个步骤的输入参数（简化版，不要太长）
- 每个步骤的执行结果（✅/❌）
- 每个步骤的耗时

##### 2.X.3 结果数据
```markdown
| 数据项 | 值 |
|--------|------|
| 关键数据1 | 实际获取的值 |
| 关键数据2 | 实际获取的值 |
```

##### 2.X.4 问题分析（如果有问题）
```markdown
| 问题 | 原因分析 | 影响范围 |
|------|----------|----------|
| 问题描述 | 可能原因 | 影响的功能 |
```

### 报告格式要求

| 要求 | 说明 |
|------|------|
| **表格优先** | 使用表格展示数据，便于阅读 |
| **简洁参数** | 参数列不要展示完整JSON，简化为关键参数 |
| **结果明确** | 使用 ✅/⚠️/❌ 标识结果状态 |
| **耗时必填** | 每个步骤的耗时必须记录 |
| **数据真实** | 展示实际获取的数据，不是假设数据 |

### 禁止的简化行为

❌ **禁止**只写"任务完成"不展示执行过程
❌ **禁止**省略工具调用参数
❌ **禁止**省略执行流程表
❌ **禁止**省略耗时信息
❌ **禁止**使用假设数据代替真实数据

### 钉钉文档API调用

**创建新文档**：
```bash
mcporter call 钉钉文档 create_document --args '{
  "title": "淘宝桌面版CLI评测报告_20260323_143052",
  "markdown": "# 评测报告内容\n\n..."
}'
```

---

## 报告模板

### 报告结构

```
淘宝桌面版CLI工具评测报告
│
├── 一、整体小结
│   ├── 1.1 评测概览表
│   ├── 1.2 总体评分
│   ├── 1.3 任务完成度表
│   ├── 1.4 工具调用总览表
│   ├── 1.5 耗时分布表
│   ├── 1.6 问题汇总表
│   └── 1.7 关键结论
│
├── 二、分任务详情
│   └── 2.X 任务X：{任务名称}
│       ├── 2.X.1 任务概要表
│       ├── 2.X.2 执行流程表
│       ├── 2.X.3 工具调用详情
│       │   ├── 调用1：工具名
│       │   │   ├── 输入参数：
│       │   │   │   ```json
│       │   │   │   {...}
│       │   │   │   ```
│       │   │   ├── 输出结果：
│       │   │   │   ```json
│       │   │   │   {...}
│       │   │   │   ```
│       │   │   └── 耗时：XXms
│       │   └── 调用2：...
│       ├── 2.X.4 过程截图
│       ├── 2.X.5 数据结果
│       ├── 2.X.6 问题分析
│       └── 2.X.7 评价与建议
│
├── 三、技术分析
│   ├── 3.1 工具调用统计表
│   ├── 3.2 性能指标表
│   └── 3.3 问题清单
│
└── 四、附录
    ├── 4.1 截图清单
    └── 4.2 完整调用日志路径
```

---

## 评分计算

```
总分 = Σ(任务评分 × 权重)
```

**任务权重**（v1.0.0）：
| 任务 | 权重 |
|------|------|
| 1. 淘金币签到 | 10% |
| 2. 商品搜索+对比+加购 | 14% |
| 3. 订单管理 | 14% |
| 4. 获取购物车以及降价信息 | 14% |
| 5. 客服咨询对话 | 10% |
| 6. 图片OCR识别与淘宝加购 | 4% |
| 7. 购物车价格监控设置 | 4% |
| 8. 退货退款申请 | 8% |
| 9. 收藏夹商家上新检查 | 6% |
| 10. 图片搜索找性价比最高商品 | 8% |
| 11. 订单评价 | 8% |
| **总计** | **100%** |

---

## 迭代记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v1.0.0 | 2026-03-23 | 重构为CLI工具评测框架；钉钉文档改为创建新文档；新增任务10、11 |

---

## 文件结构

```
~/.copaw/tasks/benchmark_{id}/
├── status.json              # 评测状态
├── timing.log               # 耗时日志
├── screenshots/             # 截图目录
│   ├── T1_S1_homepage.png
│   └── ...
├── calls/                   # 工具调用日志
│   ├── T1_S1_navigate.json
│   └── ...
├── tasks/                   # 任务详情
│   ├── task_1.json
│   └── ...
└── 评测报告.md              # Markdown报告
```

---

*最后更新：2026-03-23 v1.0.0*